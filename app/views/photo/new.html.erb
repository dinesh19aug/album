NEW PAGE
<%= form_for @photo ,:url => { :action => "create" } do |f| %>
	<%= f.label :pic_name%>
	<%= f.text_field :pic_name%><br>

	<%= f.label :description%>
	<%= f.text_field :description%><br>
	Album name <%= select("photo", "albums_id", Albums.all.collect {|a| [ a.album_name, a.id ] }, { :include_blank => true }) %><br>
	
	<%= f.submit%>
<%end%>
