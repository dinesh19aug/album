NEW PAGE
<%= form_for @photo ,:url => { :action => "create" } do |f| %>
	<%= f.label :pic_name%>
	<%= f.text_field :pic_name%>

	<%= f.label :description%>
	<%= f.text_field :description%>
	
	<%= f.submit%>
<%end%>
